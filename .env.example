# Base URL of the n8n REST API (must end with a slash)
N8N_URL=http://localhost:5678/api/v1/

# Optional API key for authenticating requests to n8n
# N8N_API_KEY=your-api-key

# Request timeout in seconds
# N8N_TIMEOUT=30.0
